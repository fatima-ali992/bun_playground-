1.Install bun add better-auth
2.create fille called it auth.ts and put inside it 
import { betterAuth } from 'better-auth'
import { prismaAdapter } from 'better-auth/adapters/prisma'

import prisma from '@/db/index'
import env from '@/env'

export const auth = betterAuth({
  database: prismaAdapter(prisma, {
    provider: 'postgresql',
  }),
  // Allow requests from the frontend development server
  trustedOrigins: ['http://localhost:5173'],
  emailAndPassword: {
    enabled: true,}
})

3.add  BETTER_AUTH_SECRET=D27gijdvth3Ul3DjGcexjcFfgCHc8jWd
BETTER_AUTH_URL=http://localhost:3000

to env file and change the localhost port to 3000 the same one we had used it 


4.run bunx @better-auth/cli generate at my-project to generate database schema that include user session .... and overwrite on the one that we already wrote it then change the id type from string to int at prisma schema file also user id 

5.connect to DB and export data 
6.enter to prisma folder first reset the DB by run by bunx prisma migrate dev  all the data will delete 
7.run prisma migrate dev to migrate everything in db  